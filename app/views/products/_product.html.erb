<% cache product do %>
<div class="col-lg-4 col-md-4 col-sm-6">
	<div class="product"><%= link_to(image_tag(product.image_url), product) %>
    <h4 id="productname"><%= link_to product.name, product %></h4>
    <p id="product-index">
        $<%= product.price %></p>
    </div>
</div>  
<% end %>